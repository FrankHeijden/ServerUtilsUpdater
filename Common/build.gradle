group = rootProject.group + '.common'
version = rootProject.version
archivesBaseName = rootProject.name + '-Common'

dependencies {
    compileOnly "com.github.FrankHeijden.ServerUtils:ServerUtils-Common:${rootProject.serverutilsVersion}"
}

shadowJar {}
